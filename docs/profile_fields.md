# Profile fields
Extera Next uses custom profile fields for some features.

## "About" field
Extera Next has option to set "About" field in profiles.

The custom profile field for this one is `xyz.extera.about`. It is a `String` value (max. 256) characters, plain text, no HTML formatting (planned in the future).